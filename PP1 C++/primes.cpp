#include <iostream>
using namespace std;
int main(){
    int a,b;
    int sum=0;
    cin >> a >> b;
    for(int i = 1;i <=1;i++ ){
    if(a%i==0 && b%i==0)
    sum+=i;
cout << "sum"; 
    }
    return 0;
}